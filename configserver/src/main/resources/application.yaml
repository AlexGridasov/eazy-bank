server:
  port: 8071

spring:
  application:
    name: "configserver"
  profiles:
     active: native
#    active: git
  cloud:
    config:
      server:
        native:
#          search-locations: "classpath:/config"
          search-locations: "file:///d://dev//config"
#        git:
#          uri: "https://github.com/eazybank/eazybank-config.git"
#          default-label: main
#          timeout: 5
#          clone-on-start: true
#          force-pull: true
